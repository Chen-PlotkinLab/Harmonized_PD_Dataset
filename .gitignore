.Rproj.user
.Rhistory
.RData
.Ruserdata

# Exclude all data and result files to save space.
# Raw data stays on the shared drive/server.
data/
results/

# Exclude renv library (but keep renv.lock)
renv/library/
renv/staging/
renv/python/
renv/activate.R